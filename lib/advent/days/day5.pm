package Advent::Days::Day5;

use v5.38.2;
use warnings;
use strict;
use Data::Dumper;

use Advent::Common;

sub runDay {
    my ($self, $questionNum, $doExample) = @_;
    my $lines = getLines(5, $questionNum, $doExample);
}
