package Advent::Days::Day0;

use v5.38.2;
use warnings;
use strict;
use Data::Dumper;

use Advent::Common;

sub runDay {
    my ($self, $questionNum, $doExample) = @_;
    my $lines = getLines(0, $questionNum, $doExample);
}
