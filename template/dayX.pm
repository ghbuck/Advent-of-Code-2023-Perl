package Advent::Days::Day0;

use v5.38.2;
use warnings;
use strict;

sub runDay {
    my ($self, $runConfig) = @_;
    my $lines = Advent::Common->getLines($runConfig);

    my $total = 0;

    return $total;
}

1;
